# AI Marketing Automation

Run the local app:

```powershell
python backend/server.py --port 8001
```

Then open `http://127.0.0.1:8001` in your browser.

The app is self-contained and uses the `AI Marketing Automation` name throughout the frontend and backend.
